{
  "name": "hello-layer",
  "version": "0.1.0",
  "description": "Sample extension description.",
  "scripts": {
    "start": "zem start",
    "build": "zem build",
    "clean": "zem clean",
    "exec": "zem exec"
  },
  "zeplin": {
    "displayName": "Styles",
    "projectTypes": [
      "ios"
    ]
  },
  "dependencies": {
    "zem": "^0.1.2"
  }
}